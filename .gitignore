
algojob-agent-server/
interview_manager/
algojob_nest/
algojobs_frontend/
apex-assessment/
interview-proctoring/
debug-assessment/
aptitude-assessment/

# Pre-rename folder names (GitHub repos renamed 2026-08-03) — keep ignored
# until every checkout has migrated via clone.sh's delete-and-reclone prompt.
algojobs_service/
algoapex-microservice/
algojob-proctoring-mise/
Algojob-debug-mise/

# Secrets. Every .env in this tree holds live credentials (Atlas, Neon, AWS,
# Razorpay, WhatsApp, LiveKit). .env.example is the committed template; real
# values come from the team's secret store — never from git.
.env
.env.local
**/.env
!.env.example

# Runtime output from dev.sh / start.sh
logs/

# Not part of the deployed stack — large local scratch areas
staging/
analysis/
pypackages/
report/

# OS / editor
.DS_Store
**/.DS_Store
.idea/
.vscode/
